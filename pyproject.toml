[project]
name = "audio-classification"
version = "0.1.0"
description = "2 models 2 represetations = 4 experiments"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "librosa>=0.11.0",
    "numpy>=2.4.2",
    "pandas>=3.0.1",
    "pytest>=9.0.2",
    "torch>=2.10.0",
]
